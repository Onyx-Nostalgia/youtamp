# ---------------------------------
# --- Gemini API Key (Required) ---
# ---------------------------------
# Your Gemini API key. This is required for the application to function.
# You can obtain a key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# ---------------------------------
# --- Development & Debugging ---
# ---------------------------------
# Log level for the application.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL="INFO"

# To test the frontend without calling the Gemini API, provide a path to a mock
# timestamp file. The path is relative to the project root.
# file MUST in the "artifacts" directory
# Example: MOCK_FILE="artifacts/SOME_VIDEO_ID/timestamps.txt"
MOCK_FILE=""

# Specify a different HTML file to render.
# Default: index.html
# HTML_FILE="index.html"

# -------------------------------------------------
# --- Prompt/Response Caching (for debugging) ---
# -------------------------------------------------
# Set to "true" to save the generated prompt to a file in the artifacts directory.
SAVE_PROMPT="false"

# Set to "true" to save the raw API response to a file in the artifacts directory.
SAVE_RESPONSE="false"

# Set to "true" to load a previously saved prompt from the artifacts directory,
# bypassing transcript fetching and prompt generation. Useful for re-running the same prompt.
LOAD_PROMPT="false"
